on:
  push:
    branches: [main]
  pull_request:
    branches: [main]  

jobs:
  test:
    name: Test
  runs-on: ubuntu@latest

  steps:
    -  name: code lao
       uses: actions/checkout@v3

    -  name: node setup
       uses: actions/setup-node@v3
       with:
       node-version: "20"
    
    -  name: test karo
       run: "node test.js"